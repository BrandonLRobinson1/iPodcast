needed npm install --save-dev babel-core -DONE

creates new branch and name
git checkout -b feat/xml

git initiats github
checkout -b creates new branch
feat/xml is the name

git push origin master

push send
origin where
name(master, feat/xml) where 
